<Page x:Class="WpfApp1.Pages.Auth"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Auth">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="120"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Пароль" FontSize="20" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10" Grid.Row="1"/>
        
        <TextBlock Text="Телефон" FontSize="20"  VerticalAlignment="Top" HorizontalAlignment="Right" Margin="10" Grid.Row="1"></TextBlock>
        <TextBox x:Name="Phone" PreviewTextInput="Phone_PreviewTextInput" FontSize="20" VerticalAlignment="Top"  Margin="10" Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left"></TextBox>
        <TextBox x:Name="Password" PreviewTextInput="Password_PreviewTextInput" FontSize="20" VerticalAlignment="Bottom"  Margin="10" Grid.Row="1" Grid.Column="1" Width="200" HorizontalAlignment="Left"></TextBox>
        <Button  Content="Войти" Height="60" Width="200" FontSize="20" Grid.Row="2" Grid.ColumnSpan="2"></Button>
    </Grid>
</Page>
