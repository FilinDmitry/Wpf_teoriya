<Page x:Class="WpfApp1.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">
    <Page.Resources>
        <CollectionViewSource x:Key="RoleSource"/>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Панель администратора" Margin="30, 0, 30, 0" TextAlignment="Center" FontSize="30"></TextBlock>
            <TextBlock Margin="5" Text="поиск" VerticalAlignment="Center" FontSize="18"></TextBlock>
            <TextBox x:Name="Search" Width="200" Height="30" Margin="5" TextChanged="Search_TextChanged"></TextBox>
        </StackPanel>
        <ListBox  x:Name="Users_LB" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="10" Padding="10">
                    <StackPanel HorizontalAlignment="Center">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Padding="15" Text="{Binding ID, StringFormat=ID: {0}}" TextWrapping="Wrap"/>
                            <TextBlock Padding="15" Text="{Binding Phone, StringFormat=Телефон: {0}}"/>
                            <TextBlock Padding="15" Text="{Binding FIO, StringFormat=ФИО: {0}}" MaxWidth="200" TextWrapping="Wrap"/>
                        </StackPanel>
                            <ComboBox x:Name="RoleChanger" Width="140" ItemsSource="{Binding AvailableRoles}" SelectedValue="{Binding SelectedRole}" Height="30" SelectionChanged="RoleChanger_SelectionChanged"></ComboBox>
                    </StackPanel>
                    </Border>
                </DataTemplate>


            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button Grid.Row="2" Margin="5" HorizontalAlignment="Center" Padding="3" FontSize="16" Content="Добавить нового пользователя" Click="Button_Click"></Button>
    </Grid>
    
</Page>

<!--<ListBox x:Name="PizzaListBox" SelectionChanged="PizzaListBox_SelectionChanged" Margin="0,0,0,55" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.ColumnSpan="2" d:ItemsSource="{d:SampleData ItemCount=5}">
<ListBox.ItemTemplate>
    <DataTemplate>
        <StackPanel HorizontalAlignment="Center">
            <Image Height="100" Width="160"  Source="{Binding Directory}"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Padding="15" Text="{Binding Price, StringFormat=Цена: {0}}"/>
                <TextBlock Padding="15" Text="{Binding Name}"/>
            </StackPanel>
            <Button Height="40" Content="Добавить в корзину" Click="Button_Click"></Button>
        </StackPanel>
    </DataTemplate>


</ListBox.ItemTemplate>
<ListBox.ItemsPanel>
    <ItemsPanelTemplate>
        <WrapPanel/>
    </ItemsPanelTemplate>
</ListBox.ItemsPanel>
</ListBox> -->