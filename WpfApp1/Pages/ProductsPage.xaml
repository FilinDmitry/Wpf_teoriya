<Page x:Class="WpfApp1.Pages.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox MouseDoubleClick="ListBox_MouseDoubleClick" x:Name="ListBox" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.ColumnSpan="2" d:ItemsSource="{d:SampleData ItemCount=5}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding Color}" BorderBrush="SeaGreen" BorderThickness="2" CornerRadius="5" Padding="3">
                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Padding="2" Text="{Binding product.Name}"/>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Padding="2" Text="{Binding Price}"/>
                                    <TextBlock TextDecorations="Strikethrough" Padding="2" Text="{Binding PriceNoDisc}"/>
                                </StackPanel>
                                <TextBlock Padding="2" Text="{Binding product.Rating, StringFormat=Рейтинг: {0}}"/>
                            </StackPanel>
                            <Button Content="Добавить в корзину" Click="Button_Click"></Button>
                            
                        </StackPanel>
                    </Border>
                </DataTemplate>


            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0, 0, 30, 0">
            <TextBlock VerticalAlignment="Center" Margin="5" FontSize="14" Text="Название: "></TextBlock>
            <TextBox x:Name="TB_Name" TextChanged="TextBox_TextChanged" FontSize="14" Width="120" Height="25"></TextBox>
            <TextBlock VerticalAlignment="Center" Margin="5" FontSize="14" Text="Тип: "></TextBlock>
            <ComboBox x:Name="CB_Type" SelectionChanged="CB_Type_SelectionChanged" FontSize="14" Width="120" Height="25"></ComboBox>
            <TextBlock VerticalAlignment="Center" Margin="5" FontSize="14" Text="Производитель: "></TextBlock>
            <TextBox x:Name="TB_Manufacturer" TextChanged="TextBox_TextChanged" FontSize="14" Width="120" Height="25"></TextBox>
        </StackPanel>
        <Button Click="Button_Click_1" Content="Перейти в корзину" Grid.Row="2"></Button>
    </Grid>
</Page>
