<Page x:Class="WpfApp1.Pages.ServicePageForMaster"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServicePageForMaster">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Margin="20" Grid.Row="1">
            <TextBlock FontSize="30" Margin="5" Text="{Binding Date}"></TextBlock>
            <TextBlock  FontSize="30" Margin="5" Text="{Binding Time}"></TextBlock>
            <TextBlock  FontSize="30" Margin="5"  Text="{Binding service.User.FIO}"></TextBlock>
            <TextBlock  FontSize="30" Margin="5"  Text="{Binding service.User.Phone}"></TextBlock>
            <TextBlock  FontSize="30" Margin="5"  Text="{Binding Type}"></TextBlock>
            <Viewbox Height="30" HorizontalAlignment="Left" Margin="10">
                <CheckBox IsChecked="{Binding service.IsComplete}" Content="Выполнен"></CheckBox>
            </Viewbox>
        </StackPanel>
        
    </Grid>
</Page>
