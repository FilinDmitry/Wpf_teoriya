<Page x:Class="WpfApp1.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="80*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>

        <ComboBox x:Name="ComboBox_Sort" VerticalContentAlignment="Center"
              Height="35" Width="125" Margin="5" SelectionChanged="SortByCB_SelectionChanged"/>
        <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" TextAlignment="Right" Margin="5" FontSize="18" Width="200" Height="30" Text="Поиск:"></TextBlock>
        <TextBox x:Name="TextBox_Search" Grid.Column="2" VerticalContentAlignment="Center" HorizontalAlignment="Left" Height="30" Width="200" TextChanged="TextBox_Search_TextChanged"/>

        <Button Grid.Column="4" Margin="20" Content="Аккаунт" Click="Profile_Click"></Button>

        <ListBox x:Name="ListBox_FilmsCatalog" SelectionChanged="FilmsLB_SelectionChanged"
             Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="4"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled" d:ItemsSource="{d:SampleData ItemCount=5}" MouseDoubleClick="ListBox_FilmsCatalog_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="205" >
                        <Image Source="{Binding Image_path}" Width="150" Height="200" />
                        <TextBlock Text="{Binding Name}" FontSize="18"/>
                        <TextBlock Text="{Binding Rating, StringFormat=Рейтинг: {0}}"/>
                        <TextBlock Text="{Binding Age_Rating.Min_Age}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
